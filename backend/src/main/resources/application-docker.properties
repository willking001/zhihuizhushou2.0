# 服务器配置
server.port=8080

# 数据库配置 (使用服务名连接)
spring.datasource.url=jdbc:mysql://mysql:3306/dianxiaozhu?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=dianxiaozhu
spring.datasource.password=dianxiaozhu123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Redis 配置 (使用服务名连接)
# 暂时禁用 Redis 自动配置以解决启动问题
# spring.redis.host=redis
# spring.redis.port=6379
# spring.redis.timeout=10000
# spring.redis.lettuce.pool.max-active=8
# spring.redis.lettuce.pool.max-wait=-1
# spring.redis.lettuce.pool.max-idle=8
# spring.redis.lettuce.pool.min-idle=0

# Kafka 配置 (使用服务名连接)
spring.kafka.bootstrap-servers=kafka:9092

# Actuator 端点配置
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always